Steps:
1. Clone the repo
2. Install selenium standalone server `npm install selenium-standalone@latest -g`
3. Install selenuim server `selenium-standalone install`
4. Start selenium standalone server `selenium-standalone start`
5. Install Allure Commandline `npm install allure-commandline -g`
6. Run `npm install`
7. Run `npm test`
